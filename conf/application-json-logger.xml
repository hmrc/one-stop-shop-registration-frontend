<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="uk.gov.hmrc.play.logging.JsonEncoder"/>
    </appender>

    <logger name="uk.gov" level="WARN"/>

    <root level="WARN">
        <appender-ref ref="STDOUT"/>
    </root>

    <logger name="application.controllers.actions.AuthenticatedIdentifierAction" level="${logger.controllers.actions.AuthenticatedIdentifierAction:-INFO}"/>
    <logger name="application.services.EmailVerificationService" level="${services.EmailVerificationService:-INFO}"/>
</configuration>
